<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Daily updates on total COVID-19 cases using data provided by Johns Hopkins CSSE, and a utility to quickly get a feel for the number of cases occurring near you.">
    <title>COVID-19 Report</title>
    <style>
        *, *:before, *:after {
            box-sizing: border-box;
        }

        header {
            margin: 12px 0;
        }
        header h1 {
            margin: 0;
        }

        main > * {
            margin-bottom: 12px;
        }
        main *:last-child {
            margin-bottom: 0;
        }

        .covid19-location-info {
            display: flex;
            padding: 12px;
        }
        .covid19-location-info button {
            font-size: 24px;
            width: 48px;
        }

        .covid19-data table {
            position: relative;
            text-align: left;
            border-spacing: 6px;
            margin-bottom: 12px;
        }
        .covid19-data table:first-child {
            float: right;
        }
        .covid19-data table:last-child {
            margin-bottom: 0;
        }
        .covid19-data table th {
            background-color: whitesmoke;
            position: sticky;
            top: 0;
        }
        .covid19-data table tr:nth-child(even) {
            background-color: white;
        }
        .covid19-data table tr:nth-child(odd) {
            background-color: whitesmoke;
        }
        .covid19-data table th, .covid19-data table td {
            padding: 6px;
        }

        footer {
            display: flex;
            flex-direction: column;
        }
    </style>
</head>